<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.transport.ideasforlife">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".IdeasLife" />
        <activity android:name=".QuotesActivity" />
        <activity android:name=".Biography" />
        <activity android:name=".LifeHack" />
        <activity android:name=".Facts" />
        <activity android:name=".NamesActivity" />
        <activity android:name=".MahatmaGandhi" />
        <activity android:name=".SwamiVivekaNand" />
        <activity android:name=".Ravindratagor"></activity>
    </application>

</manifest>